'use strict';

/**
 * course-section-type router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::course-section-type.course-section-type');
