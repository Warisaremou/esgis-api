'use strict';

/**
 * course-section-type controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::course-section-type.course-section-type');
